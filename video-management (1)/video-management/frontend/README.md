# Video Management Frontend

## Setup
1. Copy `.env.example` to `.env`.
2. Set the same `VITE_ADMIN_API_KEY` as the backend `ADMIN_API_KEY`.
3. Run `npm install`.
4. Run `npm run dev`.

Open the Vite URL shown in the terminal.
